'use client'
import {useState} from 'react'
export default function FeedbackForm({courseId}:{courseId:string}){const[rating,setRating]=useState(5);const[comment,setComment]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function submit(){setBusy(true);const r=await fetch('/api/feedback',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({courseId,rating,comment})});const d=await r.json();setBusy(false);setMsg(r.ok?'Feedback submitted.':d.error??'Unable to submit feedback.');if(r.ok)setComment('')}return <div style={{marginTop:20}}><label>Rating <select value={rating} onChange={e=>setRating(Number(e.target.value))}>{[5,4,3,2,1].map(v=><option key={v} value={v}>{v}/5</option>)}</select></label><textarea value={comment} onChange={e=>setComment(e.target.value)} rows={4} placeholder="Share feedback" style={{display:'block',width:'100%',marginTop:8,padding:10}}/><button onClick={submit} disabled={busy} style={{marginTop:8,padding:'8px 12px'}}>{busy?'Submitting…':'Submit feedback'}</button>{msg&&<p>{msg}</p>}</div>}
