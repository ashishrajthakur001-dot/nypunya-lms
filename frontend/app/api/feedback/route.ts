import { cookies } from 'next/headers'
import { NextResponse } from 'next/server'
const URL=process.env.NEXT_PUBLIC_SUPABASE_URL ?? 'https://yguicuybvsvjsneftjpb.supabase.co'
const KEY=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY ?? 'sb_publishable_9UE80CKA8CCc8auw_HBQQw_dzgzeanf'
export async function POST(request:Request){const token=cookies().get('nypunya_access_token')?.value;if(!token)return NextResponse.json({error:'Sign in required.'},{status:401});const {courseId,rating,comment}=await request.json();if(!courseId||!Number.isInteger(rating)||rating<1||rating>5)return NextResponse.json({error:'Course and a rating from 1 to 5 are required.'},{status:400});const headers={apikey:KEY,Authorization:`Bearer ${token}`,'Content-Type':'application/json'};const userResponse=await fetch(`${URL}/auth/v1/user`,{headers});if(!userResponse.ok)return NextResponse.json({error:'Session expired.'},{status:401});const user=await userResponse.json();const response=await fetch(`${URL}/rest/v1/feedback`,{method:'POST',headers,body:JSON.stringify({course_id:courseId,student_id:user.id,rating,comment:comment?.trim()||null})});if(!response.ok)return NextResponse.json({error:await response.text()},{status:500});return NextResponse.json({ok:true})}
