'use client';
import {useEffect,useState} from 'react';
import {supabase} from '../lib/supabase';
type Course={id:string;title:string;description:string;status:string};
export default function Home(){const [courses,setCourses]=useState<Course[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{supabase().from('courses').select('id,title,description,status').eq('status','PUBLISHED').order('created_at').then(({data})=>{setCourses(data||[]);setLoading(false)})},[]);return <main><nav><strong>NypunyaLMS</strong><span>Learning Management System</span><button>Student Portal</button></nav><section className="hero"><div><p className="eyebrow">LEARN • PRACTICE • GROW</p><h1>One place for the complete learning journey.</h1><p>Courses, assignments, assessments, progress and certificates — brought together in a role-based LMS prototype.</p><div><button className="primary">Explore Courses</button><button className="secondary">Trainer Portal</button></div></div><div className="card stat"><b>3</b><span>Prototype courses</span><hr/><b>3</b><span>Core learner roles</span><hr/><b>100%</b><span>Progress-ready</span></div></section><section className="section"><div className="sectionHead"><div><p className="eyebrow">COURSE CATALOG</p><h2>Featured learning paths</h2></div><span>Powered by Supabase</span></div>{loading?<p>Loading courses…</p>:<div className="grid">{courses.map(c=><article className="card" key={c.id}><span className="pill">{c.status}</span><h3>{c.title}</h3><p>{c.description}</p><button className="link">View course →</button></article>)}</div>}</section><section className="section roles"><div><p className="eyebrow">ROLE-BASED EXPERIENCE</p><h2>Built around the NypunyaLMS workflow</h2></div><div className="grid"><article><h3>Student</h3><p>Enroll, consume resources, submit assignments, take assessments and track progress.</p></article><article><h3>Trainer</h3><p>Manage courses, content, assignments, quizzes and learner evaluation.</p></article><article><h3>Admin</h3><p>Manage users, trainers, courses, batches, certificates, feedback and reports.</p></article></div></section><footer>Prototype synthesized from the supplied NypunyaLMS resume context.</footer></main>}