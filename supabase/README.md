# Supabase

The prototype uses the Supabase project configured for NypunyaLMS. The initial schema was provisioned in that project with tables for profiles, courses, enrollments, assignments, submissions, quizzes, certificates and feedback, plus indexes and Row Level Security.

Keep service-role credentials out of source control. Only the public project URL and anon key belong in the web app environment.
